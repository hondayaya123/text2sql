---
name: gen-dict
description: 從 Oracle 資料庫自動生成業務字典 YAML。當使用者說「生成字典」、「gen-dict」、「掃描資料庫結構」、「建立字典」時使用。產生 dict/schema/（自動）和 dict/business/（LLM 推測第一版）兩層 YAML。
argument-hint: "[schema-name] [module1,module2,...]"
allowed-tools: mcp__oracle__search_tables_schema mcp__oracle__get_table_schema mcp__oracle__get_table_constraints mcp__oracle__run_sql_query
effort: high
---

## Step 1：確認目標 Schema 和業務分類

若使用者未提供 `$ARGUMENTS`，詢問：

```
請提供以下資訊：
1. Oracle Schema 名稱（例如：SEMICON、HR、SALES）
2. 業務領域分類（例如：CP/SP/WAT/LQC/MQC/RTM/HIST）
3. 各分類對應的表名前綴或關鍵字（沒有前綴則填 ALL 掃全部）
```

若有提供 `$ARGUMENTS`，第一個參數為 schema，第二個為逗號分隔的模組清單。

---

## Step 2：掃描所有資料表

**2a. 取得全部表清單、筆數、Table Comment：**
```sql
SELECT t.TABLE_NAME, NVL(t.NUM_ROWS, 0) AS NUM_ROWS,
       c.COMMENTS AS TABLE_COMMENT
FROM ALL_TABLES t
LEFT JOIN ALL_TAB_COMMENTS c
  ON c.OWNER = t.OWNER AND c.TABLE_NAME = t.TABLE_NAME
WHERE t.OWNER = '<SCHEMA>'
ORDER BY TABLE_NAME
```

**2b. 取得所有欄位 Comment：**
```sql
SELECT TABLE_NAME, COLUMN_NAME, COMMENTS
FROM ALL_COL_COMMENTS
WHERE OWNER = '<SCHEMA>'
  AND COMMENTS IS NOT NULL
ORDER BY TABLE_NAME, COLUMN_NAME
```

**2c. 取得所有 FK 約束（若有設定）：**
```sql
SELECT ac.TABLE_NAME, acc.COLUMN_NAME,
       arc.TABLE_NAME AS REF_TABLE,
       arcc.COLUMN_NAME AS REF_COLUMN
FROM ALL_CONSTRAINTS ac
JOIN ALL_CONS_COLUMNS acc
  ON acc.OWNER = ac.OWNER AND acc.CONSTRAINT_NAME = ac.CONSTRAINT_NAME
JOIN ALL_CONSTRAINTS arc
  ON arc.CONSTRAINT_NAME = ac.R_CONSTRAINT_NAME AND arc.OWNER = ac.R_OWNER
JOIN ALL_CONS_COLUMNS arcc
  ON arcc.CONSTRAINT_NAME = arc.CONSTRAINT_NAME
 AND arcc.OWNER = arc.OWNER AND arcc.POSITION = acc.POSITION
WHERE ac.CONSTRAINT_TYPE = 'R'
  AND ac.OWNER = '<SCHEMA>'
ORDER BY ac.TABLE_NAME, acc.POSITION
```

**2d. 對每個業務分類搜尋相關表：**
```
search_tables_schema("CP")  → 找含 CP 的表
search_tables_schema("WAT") → 找含 WAT 的表
（每個分類執行一次，若分類為 ALL 則跳過此步）
```

---

## Step 3：取每張表的詳細欄位結構

對每張表呼叫：
- `get_table_schema(TABLE_NAME)` → 欄位名、型別、nullable
- `get_table_constraints(TABLE_NAME)` → PK 約束

**自動偵測特殊欄位（半導體常見）：**

| 類型 | 欄位名稱特徵 |
|------|------------|
| 軟刪除 | 名稱含 DEL / DELETE / VALID / INACTIVE / FLAG / ACTIVE |
| 建立時間 | CREATE_TIME / CREATE_DATE / CTIME / INS_DATE |
| 更新時間 | UPDATE_TIME / MOD_DATE / UPD_DATE / MTIME |
| 批號 | LOT_ID / LOT_NO / LOT_NUMBER |
| 晶圓號 | WAFER_ID / WAFER_NO |
| 廠別 | FAB_ID / FAB / SITE / PLANT / FACTORY |
| 產品 | PRODUCT_ID / PROD_ID / PART_NO |
| 工程師 | ENG_ID / ENGINEER_ID / USER_ID |

---

## Step 4：LLM 推理 Comment 和 JOIN 關係

### 4a. 推理空白的 Table Comment

若 TABLE_COMMENT 為空，根據表名推理：

**命名慣例對照：**
```
前綴_LOT      → {前綴}測試/製程批次主檔
前綴_WAFER    → 晶圓層級資料
前綴_DIE      → Die層級資料
前綴_PARAM    → 參數定義主檔
前綴_RESULT   → 量測/測試結果明細
前綴_SUMMARY  → 統計彙總資料
前綴_SPEC     → 規格定義
前綴_BIN      → Bin分類資料
前綴_HIST     → 歷史紀錄
前綴_MAP      → 對應/映射關係
```

標記：`inferred: true`（需人工確認）

### 4b. 推理空白的 Column Comment

若 COLUMN_COMMENT 為空，根據欄位名推理：

**半導體欄位命名慣例：**
```
LOT_ID / LOT_NO     → 批次編號
WAFER_ID / WAFER_NO → 晶圓片號（通常 01~25）
DIE_X / DIE_Y       → Die 在晶圓上的座標
SITE_NO / SITE_ID   → 測試座位編號
PARAM_ID / PARAM_NO → 量測參數代碼
MEAS_VALUE          → 量測值
TARGET / NOMINAL    → 目標值
USL / UPPER_SPEC    → 規格上限（Upper Spec Limit）
LSL / LOWER_SPEC    → 規格下限（Lower Spec Limit）
UCL / LCL           → 管制上下限（Control Limit）
YIELD               → 良率（%）
PASS_DIE / FAIL_DIE → 通過/失敗 Die 數
TOTAL_DIE / GROSS_DIE → 總 Die 數
BIN_NO / BIN        → Bin 分類編號
PASS_FAIL           → 測試結果（P=Pass / F=Fail）
PRODUCT_ID / PROD_ID→ 產品代碼
PROCESS_NODE        → 製程節點（如 28nm）
FAB_ID / FAB        → 晶圓廠代碼
ENG_ID / ENGINEER   → 負責工程師代碼
VALID_FLAG          → 有效旗標（1=有效 / 0=作廢）
DEL_FLAG / IS_DEL   → 刪除旗標
CREATE_TIME         → 建立時間
UPDATE_TIME         → 最後更新時間
```

對無法判斷的欄位：填入 `"[待補充] 欄位用途不明"` 並標記 `inferred: false`。

### 4c. 推理 JOIN 關係（無 FK 時）

**策略一：完全相同欄位名**
掃描所有表，找出在多張表都出現的欄位名：
```
LOT_ID 出現在：CP_LOT(PK), CP_WAFER, CP_BIN_SUMMARY, WAT_LOT...
→ 這些表透過 LOT_ID 相互關聯
```

**策略二：欄位名 = 其他表名去除前綴 + _ID**
```
CP_WAFER 有欄位 LOT_ID
→ 找名稱含 LOT 的表 → 找到 CP_LOT
→ 推斷：CP_WAFER.LOT_ID → CP_LOT.LOT_ID
```

**策略三：主表/明細表語意推理**
```
表名規律：
  前綴_LOT   = 主表（批次層）
  前綴_WAFER = 明細（晶圓層，FK → LOT）
  前綴_DIE   = 明細（Die層，FK → WAFER）
  前綴_RESULT = 量測結果（FK → LOT + WAFER + PARAM）
  前綴_PARAM  = 參數定義主表（被 RESULT 參照）
```

**策略四：實際查資料驗證推斷**
對推斷的 JOIN，執行驗證查詢確認值域有重疊：
```sql
SELECT COUNT(*) FROM CP_WAFER w
WHERE NOT EXISTS (
  SELECT 1 FROM CP_LOT l WHERE l.LOT_ID = w.LOT_ID
)
```
若結果接近 0 → JOIN 關係成立；若有大量孤兒資料 → 標記為可疑。

---

## Step 5：寫出 dict/schema/\<TABLE_NAME\>.yaml

每張表一個檔案：

```yaml
# AUTO-GENERATED by /gen-dict — 請勿手動修改
# Generated: <ISO timestamp>
table: <TABLE_NAME>
db_schema: <SCHEMA>
estimated_rows: <NUM_ROWS>
db_comment: "<TABLE_COMMENT>"
comment_inferred: <true|false>   # true = LLM 推理，false = DB 原有

columns:
  <COLUMN_NAME>:
    type: <DATA_TYPE>
    nullable: <Y/N>
    db_comment: "<COMMENT>"
    comment_inferred: <true|false>

foreignKeys:
  - column: <COL>
    refTable: <REF_TABLE>
    refColumn: <REF_COL>
    source: <db_constraint|naming_convention|semantic_inference>
    confidence: <high|medium|low>

primaryKey: [<PK_COLS>]

autoDetected:
  softDeleteColumn: <欄位名 or null>
  timestampColumns: [<欄位名列表>]
  batchColumns: [LOT_ID, WAFER_ID]
  siteColumn: <欄位名 or null>

reviewItems:
  - <需要人工確認的項目描述>
```

---

## Step 6：寫出 dict/business/module_\<id\>.yaml

```yaml
module: <MODULE_ID>
version: "<YYYY-MM-DD>"
description: <推測的業務說明>
description_inferred: true     # 提醒人工確認

triggerKeywords:
  - <模組ID>
  - <推測的中文關鍵詞>

businessRules:
  - rule: <推測的規則名稱>
    sqlPattern: "<Oracle SQL 條件>"
    appliesTo: [<表名>]
    confidence: auto           # LLM推測，需人工確認
    note: "<推測依據說明>"

tables:
  <TABLE_NAME>:
    description: <推測或 DB comment>
    description_inferred: <true|false>
    columns:
      <COLUMN_NAME>:
        desc: <推測或 DB comment>
        inferred: <true|false>

joinPatterns:
  - scenario: <JOIN 場景說明>
    sql: "<TABLE_A a JOIN TABLE_B b ON b.FK = a.PK>"
    source: <db_constraint|naming_convention|semantic_inference>
    confidence: <high|medium|low>
    verified: <true|false>     # 是否已用查詢驗證

queryPatterns:
  - id: QP-<MODULE>-001
    description: <推測的常見查詢>
    triggers: [<觸發關鍵詞>]
    sql: |
      SELECT ...
      FROM ...
      WHERE ...
      FETCH FIRST 50 ROWS ONLY
    confidence: auto
```

---

## Step 7：寫出 dict/\_index.yaml

```yaml
# AUTO-GENERATED by /gen-dict
modules:
  - id: <MODULE_ID>
    description: <推測描述>
    triggerKeywords: [<關鍵詞列表>]
    file: business/module_<id>.yaml
```

---

## Step 8：寫出 dict/\_global\_rules.yaml

```yaml
dialect: oracle

forbiddenSyntax:
  - "LIMIT n        → 改用 FETCH FIRST n ROWS ONLY"
  - "$1 $2          → 改用 :param_name"
  - "DATE_TRUNC     → 改用 TRUNC(col, 'MM')"
  - "NOW()          → 改用 SYSDATE"
  - "information_schema → 改用 ALL_TABLES / ALL_TAB_COLUMNS"

extraRules:
  - "禁止 INSERT / UPDATE / DELETE / DDL"
  - "多表查詢必須有明確 JOIN ON 條件"
  - "分頁用 FETCH FIRST n ROWS ONLY"
  - "預設最多回傳 100 筆"
```

---

## Step 9：輸出摘要報告

```
=== /gen-dict 完成 ===

schema YAML（dict/schema/）：共 XX 張表
  ✅ CP_LOT.yaml          (DB comment 齊全)
  ⚠️  CP_WAFER.yaml        (TABLE comment 為 LLM 推理)
  ⚠️  WAT_RESULT.yaml      (所有 column comment 為 LLM 推理)

business YAML（dict/business/）：共 X 個模組
  module_cp.yaml    (含 X 張表)
  module_wat.yaml   (含 X 張表)

JOIN 關係來源統計：
  db_constraint       : X 個  (最可靠)
  naming_convention   : X 個  (請確認)
  semantic_inference  : X 個  (請人工驗證)

⚠️  需人工確認（reviewItems）：
  [ ] CP_LOT.VALID_FLAG — 有效值是 '1' 還是 'Y'？
  [ ] CP_WAFER → CP_LOT JOIN — 命名慣例推斷，建議執行驗證查詢
  [ ] WAT_RESULT 所有 column comment — LLM 推理，請確認正確性
  [ ] 以下表未能判斷模組：SYSTEM_LOG、TEMP_CALC

下一步：確認 YAML 內容後執行 /build-dict 向量化存入搜尋引擎
```
